# .NET build artifacts
**/bin/
**/obj/

# Inno Setup output
installer/Output/

# Logs générés à l'exécution
logs/
*.log

# Config runtime (paths locaux)
runtime_config.json

# Certificat de signature — clé privée non versionnée
# Le .cer public (certs/Astralcodes.cer) est lui versionné
certs/*.pfx
certs/*.p12
*/certs/*.cer
# Fichiers temporaires
*.pid
*.tmp

# Claude Code internal
.claude/

# Dossiers développement local — trackés sur leurs branches dédiées
# (branch SysViewManager / branch Installeur)
SysViewManager/
scripts/
installer/